// mulitplies the element of an array by 2 and return modified array

function mulitpliesArray(muliplier : number, arr:number[]):number[]{
    let newArr : number[]= [];
    // for (let i = 0; i < arr.length; i++) {
        
    //     newArr.push(arr[i] * 2);
    // }
    // arr.forEach(element => {
    //    newArr.push(element * 2); 
    // });

    

    return newArr;
}

console.log(mulitpliesArray(2,[1,2,3]));