"use strict";
// mulitplies the element of an array by 2 and return modified array
function mulitpliesArray(muliplier, arr) {
    let newArr = [];
    // for (let i = 0; i < arr.length; i++) {
    //     newArr.push(arr[i] * 2);
    // }
    // arr.forEach(element => {
    //    newArr.push(element * 2); 
    // });
    return newArr;
}
console.log(mulitpliesArray(2, [1, 2, 3]));
